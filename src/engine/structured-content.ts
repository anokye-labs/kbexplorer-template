/**
 * Shim — re-exports from `@anokye-labs/kbexplorer-engine` (moved in
 * anokye-labs/kbexplorer-template#472, slice 1/5).
 */
export {
  DEFAULT_STRUCTURED_CONTENT_PATH,
  normalizeRepoRelativeDir,
  resolveStructuredContentPath,
  hasExplicitStructuredContentPath,
} from '@anokye-labs/kbexplorer-engine';
export type { StructuredContentConfig } from '@anokye-labs/kbexplorer-engine';