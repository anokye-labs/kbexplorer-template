# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.playwright-cli/
test-results/
playwright-report/
playwright-report-gitea/
playwright-report-full-loop/
e2e/artifacts/
.env.kbexplorer

# Gitea Digital Twin Universe (DTU) harness runtime state — never committed.
# Holds the live Gitea coords + admin API token and the cached `tea` binary.
.dtu/

# Design-review screenshot artifacts — generated by `npm run capture:review`.
# The capture script (scripts/capture-review.mjs) and surface manifest
# (scripts/review-surfaces.json) ARE committed; the images are not.
review/screenshots/
